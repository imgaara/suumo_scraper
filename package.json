{
  "private": true,
  "scripts": {
    "test": "vendor/bin/phpunit",
    "test:watch": "watch 'npm run --silent test' --ignoreDotFiles ."
  },
  "license": "MIT",
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
